# Preferences backup and restore

This work item defines a user-facing workflow for sharing, restoring, and
resetting CaveViewer preferences without requiring users to locate or edit the
application's private configuration directory.

## Master plan

<style>
table th,
table td {
  vertical-align: top;
}
</style>

| ID | Description | Problem | Current implementation | Desired solution | Task details | Branch | Status |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 1 | Establish the portable preferences file contract. | Users need to exchange settings safely, but an ambiguous or unstable file contract could import unsupported values, expose machine-local paths unexpectedly, or make future changes difficult. | `Preferences` is an immutable schema snapshot. Persistence writes all declared values as an unversioned JSON object, missing and invalid values resolve independently to defaults, and unknown keys are ignored. Path preferences may contain machine-specific recording and map-library locations. | 1. A portable preferences file is a UTF-8 JSON object named `preferences.json` containing only declared preference keys.<br><br>2. Missing or invalid individual values resolve to their current defaults while other valid imported values remain intact.<br><br>3. Unknown keys are ignored so an export from a newer release can be imported safely by an older compatible release.<br><br>4. Unreadable, malformed, or non-object JSON is rejected with a clear message and does not change current or saved preferences.<br><br>5. Machine-local path preferences are included in a complete export; unavailable imported paths independently fall back to defaults without creating or authorizing directories. | 1. Define a GUI-free import/export result model around the existing preference schema and resolver.<br><br>2. Include `recording_dir` and `map_library_dir` in the complete snapshot and validate them on import so unavailable locations use destination-machine defaults.<br><br>3. Keep parsing bounded, accept only a JSON object, normalize declared fields, and report which fields used defaults without exposing raw file contents in logs.<br><br>4. Add focused tests for valid round trips, Unicode, missing fields, invalid values, unknown fields, malformed/non-object JSON, oversized input, path values, and no-partial-change failures.<br><br>5. Verification: focused transfer/persistence tests passed (150 passed); the complete applicable Windows suite passed (1,976 passed, 23 skipped, 1 symlink-privilege deselection); compileall and `git diff --check` passed. | `feat/preferences-backup-restore` | Complete — verified and ready to commit |
| 2 | Rename application preference persistence to `preferences.json`. | The internal filename `advanced_settings.json` is misleading and difficult to explain to users, but prominent existing users already depend on that file and must not lose their saved configuration during the rename. | `preferences_file()` resolves `advanced_settings.json` and calls `migrate_preference_file`, which copies `~/.caveviewer/advanced_settings.json` or `~/.caveviewer_advanced_settings.json` into the current configuration directory. Documentation and tests encode those names. | 1. CaveViewer reads and writes `<config-dir>/preferences.json` as the sole current application-preference file.<br><br>2. When `preferences.json` is absent and `<config-dir>/advanced_settings.json` exists, CaveViewer performs a one-time, lossless rename to `preferences.json` before loading preferences.<br><br>3. When both files exist, `preferences.json` takes precedence and `advanced_settings.json` remains untouched; the migration never overwrites the current file.<br><br>4. If the rename fails, CaveViewer preserves `advanced_settings.json`, loads it for the current run when readable, reports the migration failure, and retries migration at the next startup.<br><br>5. CaveViewer does not discover or migrate `.caveviewer_advanced_settings.json` or any other older preference filename.<br><br>6. Atomic preference writes and failure-cleanup guarantees remain unchanged for `preferences.json`. | 1. Change the current preference path to `<config-dir>/preferences.json` and add a narrowly scoped one-time migration from the sibling `advanced_settings.json`.<br><br>2. Make migration non-destructive: never overwrite `preferences.json`, never delete the source before the destination is published, and retain a readable fallback if the rename fails.<br><br>3. Remove support for `.caveviewer_advanced_settings.json` and other older preference paths while retaining unrelated UI-state migration behavior.<br><br>4. Update all filename-dependent tests, fixtures, benchmarks, documentation, and packaging assumptions.<br><br>5. Add regression tests for successful migration, both-files precedence, migration retry/failure fallback, malformed legacy content, and ignored older names on Windows, macOS, Linux/XDG, and `CAVEVIEWER_HOME` paths.<br><br>6. Verify atomic write failure cleanup and document the one-time compatibility migration before implementing the UI workflow.<br><br>7. Verification: focused persistence/migration/benchmark tests passed (154 passed); the complete applicable Windows suite passed (1,984 passed, 23 skipped, 1 symlink-privilege deselection); compileall and `git diff --check` passed. | `feat/preferences-backup-restore` | Complete — verified and ready to commit |
| 3 | Add Preferences backup and restore actions. | Non-technical users cannot locate the private configuration file, so they cannot easily share optimized streaming/import settings or recover a known configuration. | The embedded Preferences surface has `Streaming`, `Import`, and `Storage` tabs plus global Apply and Cancel actions. It has no file chooser, portable export, import preview, or default-restoration action. The existing `Import` tab configures map import behavior, so placing preference-file import there would be ambiguous. | 1. Preferences contains a fourth **Backup and Restore** tab with **Export preferences…**, **Import preferences…**, and **Restore defaults** actions.<br><br>2. Export opens a native Save dialog outside the private configuration directory, suggests `preferences.json`, writes atomically, and reports success or failure without closing Preferences.<br><br>3. Import opens a native Open dialog at a user-visible or last-used location, validates the selected file, and stages the resolved values in the form for review without saving until **Apply** is pressed.<br><br>4. Invalid imported fields visibly identify that defaults were substituted, while a malformed file leaves the form and saved preferences unchanged.<br><br>5. Restore defaults requires confirmation and stages current defaults in the form; **Cancel** discards the staged reset and **Apply** persists it.<br><br>6. Keyboard access, focus order, accessible names, native-dialog cancellation, narrow layouts, and scaled text follow existing Preferences conventions. | 1. Extend the Tk-free Preferences workflow with import, export, and restore-default operations and transactional outcomes.<br><br>2. Add platform-owned native open/save file selection without coupling core preference parsing to Tk.<br><br>3. Add the **Backup and Restore** tab to `PreferencesPanel`, keeping feedback in the existing action/notice region and preserving Apply/Cancel semantics.<br><br>4. Prevent destructive replacement on picker cancellation, parse/validation failure, save failure, or confirmation cancellation.<br><br>5. Add focused controller, workflow, platform-adapter, and rendering tests for success, cancellation, overwrite confirmation, invalid-field fallback, malformed files, write failures, exact export content, unsaved edits, restore defaults, keyboard focus, and responsive layouts.<br><br>6. Verification: focused transfer/workflow/presentation/platform tests passed (196 passed, 8 Linux-portal skips); the complete applicable Windows suite passed (1,993 passed, 23 skipped, 1 symlink-privilege deselection); compileall and `git diff --check` passed. | `feat/preferences-backup-restore` | Complete — verified and ready to commit |
| 4 | Document and verify the complete workflow. | Unit behavior alone does not establish that users can find exported files, understand fallback behavior, or safely move preferences between supported desktops. | User documentation explains where `advanced_settings.json` is stored and how preferences are applied, but does not describe backup, restore, default reset, filename compatibility, or native chooser behavior. | 1. User documentation explains **Preferences > Backup and Restore**, the `preferences.json` filename, imported-value fallback, restore-default staging, and the one-time migration from `advanced_settings.json`.<br><br>2. Documentation clearly states that `.caveviewer_advanced_settings.json` and other older names are unsupported.<br><br>3. Automated coverage verifies schema, persistence, migration, workflow, presentation, and platform-dialog boundaries.<br><br>4. Manual platform checks confirm that open/save dialogs start in user-visible locations and that exported files are easy to locate on Windows, macOS, and Linux.<br><br>5. The final work record includes focused and complete test evidence plus any unavailable platform validation. | 1. Update README and development references that mention `advanced_settings.json` or legacy preference migration.<br><br>2. Run focused preference/core/GUI tests, then the complete suite, the syntax check, and `git diff --check`.<br><br>3. Manually verify the one-time filename migration, export, import, invalid-value fallback, malformed-file rejection, restore defaults, Apply, and Cancel on each supported desktop; record unavailable platforms explicitly.<br><br>4. Confirm no release workflow executes or depends on the old filename; if one does, add the smallest workflow-contract regression coverage.<br><br>5. Record the commit, pull request, merge result, and remaining external action in this table.<br><br>6. Verification: focused end-to-end preference tests passed (207 passed, 8 Linux-portal skips); the complete applicable Windows suite passed (1,993 passed, 23 skipped, 1 symlink-privilege deselection); compileall and `git diff --check` passed. Native Windows, macOS, and Linux chooser smoke tests remain external because this automated session did not launch desktop dialogs and did not run on macOS/Linux. No release workflow references the old filename. | `feat/preferences-backup-restore` | Complete — verified and ready to commit |

Implementation commits: Task 1 `c2f782e`; Task 2 `894c89d`; Task 3
`c8382ba`. The intentional removal of the prior troubleshooting work artifacts
was committed separately as `f743973` before Task 1.

## Compatibility decision

The rename to `preferences.json` includes one compatibility bridge for existing
users: a sibling `advanced_settings.json` is renamed once when no
`preferences.json` exists. The current filename always wins when both exist,
and a failed migration must preserve and temporarily read the old file so user
settings are not lost. The older `.caveviewer_advanced_settings.json` name and
all other historical names remain unsupported. Release notes must explain the
new filename, automatic one-time migration, precedence rule, and unsupported
older paths.

## Validation commands

Focused validation while implementing:

```powershell
.venv\Scripts\python.exe -m pytest -p no:cacheprovider -q tests/unit/gui/test_preferences.py tests/unit/gui/test_preferences_form.py tests/unit/gui/test_preferences_workflow.py tests/unit/gui/test_preference_paths.py
```

Complete validation:

```powershell
.venv\Scripts\python.exe -m pytest -p no:cacheprovider -q
```

Syntax and diff validation:

```powershell
$env:PYTHONPYCACHEPREFIX = Join-Path $env:TEMP "caveviewer-pycache"
.venv\Scripts\python.exe -m compileall -q src/caveviewer
Remove-Item Env:PYTHONPYCACHEPREFIX
git diff --check
```
