"""GUI package for CaveViewer presentation and desktop integration.

Modules in this package may coordinate with ``caveviewer.core`` and local GUI
helpers, but should not import upward into ``caveviewer.app``. Platform-specific
decisions belong behind ``caveviewer.gui.platform`` adapters.
"""
